/**
 * Convert Js Date to iso
 * toISOString() method is used to convert javascript date to ISO standard.
 * It converts JavaScript Date object into a string, using the ISO standard.
 * 
 * Both are same??
 */

let date = new Date();
console.log(date);
let n = date.toISOString();
console.log(n);

